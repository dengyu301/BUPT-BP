package test1;
import java.lang.*;
/**
 * Created by hongqiaoli on 2/3/16.
 */
/*
public class hashmap
{
    Integer id;
    String word;
    hashmap(Integer idtmp,String wordtmp) {
        id = idtmp;
        word = wordtmp;
    }
    public int hashCode(){
        Integer a = Integer.valueOf(id);
        Integer b = Integer.valueOf(word);
        return 31 * a + b;
    }
    public boolean equals(hashmap c) {
        if (c == null)
            return false;
        if (this.id.equals(c.id) && this.word.equals(c.word))
            return true;
        return false;
    }
}
*/

public class test1 {
    public static void main(String[] args) {
        System.out.println("Hello! World!");
    }
}